# Frontend environment variables for Vite
# Copy to .env and adjust values.
# Base URL of backend API (no trailing slash). For production (e.g. Render / Railway / EC2 / custom domain)
VITE_API_BASE_URL=https://your-backend-domain.com/api

# Example: if deploying backend separately at https://vn-travels-api.onrender.com
# VITE_API_BASE_URL=https://vn-travels-api.onrender.com/api
